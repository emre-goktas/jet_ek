.venv/
venv
__pycache__/
*.pyc
*.pyo
*.pyd
backend/storage/*
!backend/storage/.gitkeep
*.pdf
.env
.DS_Store
notes/
.aider*
testResults/*.md

tests/
.pytest_cache/
# backend/data/ holds real app assets (templates.json, the .docx templates) that
# must ship with the repo — only the dynamic/runtime parts are ignored.
backend/data/*.db
backend/data/*.db-journal
backend/data/*.log

reports/*

CLAUDE.md